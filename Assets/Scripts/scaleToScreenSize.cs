using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scaleToScreenSize : MonoBehaviour
{
    float baseHeight = 2160;
    float baseWidth = 1080;

    void Start()
    {

        //gameObject.transform.localScale = Vector3.one * (baseHeight / Screen.height);
    }
}
